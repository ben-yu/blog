// Generated by CoffeeScript 1.4.0
(function() {
  var NB_TANKS, Vector3, addTank, chars, game, startSelfTank;

  Vector3 = THREE.Vector3;

  game = window.GAME;

  NB_TANKS = 4;

  chars = new Backbone.Collection;

  /*
  var message = $('#message');
  function death () {
    message.show().text("Game Over");
    setTimeout(function() {
      message.hide();
      startSelfTank();
    }, 1000);
  }
  */


  startSelfTank = function() {
    var myChar;
    return myChar = new Tank;
  };

  addTank = function() {
    var tank;
    tank = new Tank;
    return game.addTank(tank);
  };

  startSelfTank();

  setInterval(function() {
    if (game.tanks.size() < NB_TANKS) {
      return addTank;
    }
  }, 2000);

  game.animate();

}).call(this);
